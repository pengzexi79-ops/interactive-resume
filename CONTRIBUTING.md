# 贡献指南

感谢你关注本项目。仓库是一份包含真实个人资料的交互式简历，贡献时请优先保护隐私、内容真实性与身份边界。

## 可以贡献的内容

- 修正文档错误、失效链接或不准确的运行说明
- 改进无障碍、响应式布局与浏览器兼容性
- 修复可复现的交互或构建问题
- 在不改变内容事实的前提下改进性能、测试和工程质量

未经本人确认，请勿替换、扩写或推断个人经历、项目数据、联系方式和能力评价。

## 开始之前

1. 搜索现有 Issue，避免重复反馈。
2. 对较大的功能或视觉改动，先通过 Issue 说明问题、范围和预期结果。
3. 不要提交密码、令牌、私钥、私人联系方式、精确住址、证件信息或包含这些内容的日志与截图。
4. 不要把项目中的姓名、肖像、简历、履历或品牌素材用于冒充身份。

## 本地开发

需要 Node.js `>= 22.13.0`、npm、Bash 和 GNU `timeout`。在 Windows 上请使用 WSL 或兼容环境。

```bash
npm ci
npm run dev
```

## 提交前检查

根据改动范围运行：

```bash
npm run lint
npm test
```

`npm test` 会执行生产构建和渲染产物测试。若环境无法运行某项检查，请在 Pull Request 中说明未运行的命令、原因和已完成的替代验证。

## Pull Request 要求

- 保持改动聚焦，不混入无关格式化或依赖升级。
- 清楚描述问题、解决方式、影响范围和测试结果。
- 视觉改动可附桌面端与移动端截图，但必须先移除敏感信息。
- 新增依赖前说明必要性、许可证和对构建体积的影响。
- 不直接修改生成目录，如 `dist/`、`portable/` 或 `.next/`。

提交贡献即表示你有权提供相关内容，并同意你的源代码贡献按本仓库的 [MIT License](LICENSE) 发布。个人肖像、简历、履历文本、联系方式与品牌素材仍受许可证文件中所述边界约束。
